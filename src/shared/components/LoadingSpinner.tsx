import { Center, Spinner } from "@chakra-ui/react"





const LoadingSpinner = () => {
    return (
        <Center h={200}>
            <Spinner size="md" />
        </Center>
    )
}

export default LoadingSpinner